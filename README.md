# Bayesian Inference

This is a more basic but way faster implementation of the MIT PV-lab's Bayesian inference code: https://github.com/PV-Lab/bayesim

Lots of features (and documentation) are missing from this work in progress, but I just wanted to demonstrate a dumbed-down version that can yield (almost) the exact same results in minutes instead of hours!
